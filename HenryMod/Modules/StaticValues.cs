using UnityEngine;

namespace PlayableTemplar.Modules
{
    internal static class StaticValues
    {
        internal const float swordDamageCoefficient = 2.8f;

        internal const float gunDamageCoefficient = 4.2f;

        internal const float bombDamageCoefficient = 16f;

		internal static readonly Color CHAR_COLOR = new Color(0.64f, 0.31f, 0.22f);

        internal static readonly Color BUFF1_COLOR = new Color(0.54f, 0.21f, 0.12f);

        internal static readonly Color BUFF2_COLOR = new Color(0.74f, 0.41f, 0.32f);

		internal static readonly Color BUFF3_COLOR = new Color(0.3f, 0.3f, 0.3f);

		internal static readonly Color BUFF4_COLOR = new Color(0.84f, 0.51f, 0.42f);

	}
}